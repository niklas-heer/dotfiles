# Global bun packages to install
# One package per line, comments and blank lines are ignored
# `chezmoi apply` will install these when this file changes

# AI coding tools
@openai/codex # OpenAI Codex CLI
tokscale # cross-platform token usage + cost tracker for AI coding tools
