- shell:
    - ['######################']
    - ['# Setup Go environment']
    - ['######################']

- shell:
    - ['mkdir -p ~/Go/{bin,src/github.com/niklas-heer}', 'Setting up $GOPATH...']
